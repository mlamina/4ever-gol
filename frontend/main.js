document.getElementById('spawnCellButton').addEventListener('click', function() {
    const userColor = getUserColor();
    const cellData = {
        // other cell data
        color: userColor
    };
    // send cellData to backend
});